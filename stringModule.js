// Named Export: Convert string to uppercase
export function toUpperCase(str) {
    return str.toUpperCase();
}

// Named Export: Convert string to lowercase
export function toLowerCase(str) {
    return str.toLowerCase();
}